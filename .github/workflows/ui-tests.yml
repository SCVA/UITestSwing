name: UITestSwing AssertJ-Swing Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run-uitestswing-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Temurin JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run UITestSwing AssertJ-Swing test
        run: |
          xvfb-run -a mvn -B -Dtest=UITestSwingTest test
